#include <bits/stdc++.h>

using namespace std;
signed main (){
  std::ios::sync_with_stdio(false);cin.tie(0);
  int a;
  cin>>a;
  if(a%2 == 0 && a>2){
    cout<<"YES";
  }else{
    cout<<"NO";
  }
  
  return 0;
}